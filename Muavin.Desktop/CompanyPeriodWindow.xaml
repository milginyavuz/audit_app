<Window x:Class="Muavin.Desktop.CompanyPeriodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Şirket / Dönem Seç"
        Height="190" Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Şirket:"/>
        <ComboBox x:Name="cbCompany" Grid.Row="0" Grid.Column="1" Height="28"
                  DisplayMemberPath="CompanyName" SelectedValuePath="CompanyCode"
                  SelectionChanged="CompanyChanged"/>

        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,0,0" Text="Dönem:"/>
        <ComboBox x:Name="cbPeriod" Grid.Row="1" Grid.Column="1" Height="28" Margin="0,10,0,0"
                  DisplayMemberPath="Display" SelectedValuePath="Key"/>

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Width="90" Margin="0,0,8,0" Content="İptal" IsCancel="True"/>
            <Button Width="120" Content="Seç" IsDefault="True" Click="OkClicked"/>
        </StackPanel>
    </Grid>
</Window>
