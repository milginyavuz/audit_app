<!-- Muavin.Desktop/EntryDetailsWindow.xaml -->
<Window x:Class="Muavin.Desktop.EntryDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fiş Detayı" Width="980" Height="520"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="8">
        <DataGrid ItemsSource="{Binding}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Hesap Kodu" Binding="{Binding HesapKodu}" Width="180"/>
                <DataGridTextColumn Header="Hesap Adı" Binding="{Binding HesapAdi}" Width="*"/>
                <DataGridTextColumn Header="Borç" Binding="{Binding Borc, StringFormat=N2}" Width="150"/>
                <DataGridTextColumn Header="Alacak" Binding="{Binding Alacak, StringFormat=N2}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
